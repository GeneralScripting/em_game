= fb_connect_async_js

.container_12

  #games.grid_4
    %h2= t('.matches')
    %ol.games= render @games
  #center.grid_5
    - if @current_games.present?
      = render 'current_games'
    - else
      = render 'next_game'
    = render 'past_games'
    
    
  #ranking.grid_3
    %h2= t('.ranking')
    %ol= render @users